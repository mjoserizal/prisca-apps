{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/spa"
      }
    }
  ],
  "routes": [
    {
      "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
    }
  ]
}
